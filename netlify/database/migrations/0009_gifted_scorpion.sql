-- Schema snapshot baseline for Drizzle Kit.
-- Migration 0008_create_memberships already creates the membership and platform_super_admin tables and types,
-- so this migration records the generated snapshot without replaying CREATE statements.
SELECT 1;